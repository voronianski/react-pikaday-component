{
  "main": "app.js",
  "scripts": {
    "start": "../node_modules/watchify/bin/cmd.js . -o bundle.js -v -d"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": ["es2015", "react", "stage-0"]
        }
      ]
    ]
  },
  "dependencies": {},
  "devDependencies": {}
}
